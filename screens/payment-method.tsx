import { Text, View } from 'react-native';
import { SafeAreaProfile } from '../components/SafeAreaProfile';

export function PaymentMethod({ navigation }: any) {
	return (
		<SafeAreaProfile navigation={navigation}>
			<></>
		</SafeAreaProfile>
	);
}
